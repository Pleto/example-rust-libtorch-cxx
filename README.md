This example demonstrates how to integrate Rust with LibTorch by dynamically linking LibTorch into a Rust project using C++ FFI via the cxx crate

## Setup Instructions
1. Build and install LibTorch
```bash
bash hack/build_libtorch.sh
```
2. Run the project
```bash
make run
```